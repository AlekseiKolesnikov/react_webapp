import React from 'react';

const AlphabetLetter = ({letter}: { letter: string}) => {
    console.log(letter)
    return (
        <div style={{

        }}>
            <div style={{}}>
                {letter}
            </div>
        </div>
    );
};

export default AlphabetLetter;